import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.adaptive.telemetry',
    appName: 'Adaptive Telemetry',
    webDir: '../web/dist'
};

export default config;
